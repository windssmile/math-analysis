# 第 12 章自学教材级重写 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将第12章重建为4个 `content_standard: 2` 单元，形成存在、迭代与误差证书闭环。

**Architecture:** Markdown正文为单一来源；`tests/test_chapter_12.py` 固化顺序、学时、v2、算法复用和边界；导航、课程地图与站点锚点同步闭合。

**Tech Stack:** Markdown、Python 3.12 unittest、Zensical modern、现有 `mathbook_examples.bisection`。

---

## 最终合同

| 顺序 | ID | 标题 | 理论+应用 |
|---|---|---|---:|
| 12.1 | `u-03-12-01` | 连续函数怎样保证取遍中间值？ | 1.75+0.25 |
| 12.2 | `u-03-12-03` | 有固定点是否意味着简单迭代会收敛？ | 1.50+0.50 |
| 12.3 | `u-03-12-02` | 怎样把有根证明变成误差可证的算法？ | 1.25+0.75 |
| 12.4 | `u-03-12-04` | 不同存在与计算证书各自保证什么？ | 1.50+0.50 |

## 执行任务

- [x] 新增失败测试，要求精确顺序、`6+2=8` 学时、v2结构、核心锚点、唯一算法源码复用及无后续微分理论。
- [x] 重写12.1：用异号嵌套区间完整证明零点定理，由 `f-y` 推介值定理。
- [x] 重写12.2：由 `g(x)-x` 证明连续自映射存在不动点，用 `g(x)=1-x` 分离存在、唯一、迭代收敛和误差证书。
- [x] 重写12.3：按问题、假设、不变量、伪代码、误差、步数、现有Python API、失败输入的顺序建立二分法。
- [x] 新增12.4：表格比较介值、二分与压缩映射证书，并用三个案例判断可声称的结论。
- [x] 更新章节导学、Zensical导航、课程地图、迁移页面清单和真实站点锚点。
- [x] 数学审查：量词、区间不变量、端点根、存在不等于唯一、残差不等于根误差、压缩结论不重证。
- [x] 运行 `make verify`，记录测试与构建结果，在第12章停止。

## 验收记录

- 4个核心单元均为 `content_standard: 2`，理论 `6` + 应用 `2` = `8` 学时。
- 每页包含2个稳定例题、2个即时检验、5道习题和7个折叠完整答案。
- 二分单元只调用 `mathbook_examples.bisection`，未复制算法实现；既有10项算法测试保持通过。
- `make verify` 通过51项测试、统一内容检查、Zensical严格构建和真实站点检查，构建报告 `No issues found`。
- 第三部在第12章闭合，未创建或改写第四部正文。
