# 第 39 章一致性审查

**日期：** 2026-07-31
**发布边界：** 第 37–39 章已发布；第 40–41 章仍为规划。

## 内容与元数据

- 四页元数据均为 `content_standard: 2`，共 4 单元、6 学时；固定 H2、完整例题、
  即时检验及精确习题答案数量均由章级测试锁定。
- 依赖只指向既有微积分基本定理、重积分、线积分和第 39 章内前序单元。

## 数学一致性

- 从小矩形逐边导出平面散度与标量旋度，固定逆时针局部环流和正外法向通量符号。
- Green 切向形式在同时适用的简单区域上逐项使用一元基本定理证明，记录 (C^1)
  邻域条件；法向形式由正切向与正外法向关系推出。
- 有限分片明确以相反方向产生内部边界抵消；多连通区域外边界逆时针、洞边界顺时针，
  每个方向敏感页均有取向检查。
- 应用覆盖面积、环流、通量、路径无关与二维不可压缩；路径无关判据写明单连通或
  全部闭路积分为零，并用穿孔域零旋度但环流 (2\pi) 的例子标出拓扑边界。
- 本章证明未借用后续 Gauss、Stokes 公式或微分形式。

## 发布面与绿门

- 导学、课程地图、依赖图、导航与 README 已同步到 177 单元、323 学时；第 40–41 章无正文。
- 章级测试：通过。
- `make verify`：通过。
- `python3.12 scripts/check_content.py`：通过。
- `zensical build --strict`：通过。
- `python3.12 scripts/check_site.py`：通过。
- `git diff --check`：通过。
