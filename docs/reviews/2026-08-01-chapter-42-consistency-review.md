# 第 42 章一致性审查

日期：2026-08-01

## 结论

第 42 章已按 5 个核心单元、7 学时（理论 5.50，应用 1.50）发布。正文在固定紧区域的
Riemann 框架内建立含参积分、联合连续导出的参数连续性、一致极限与积分交换，并用
连续移动尖峰证明逐点收敛本身不足以交换极限和积分。

## 数学审查

1. 连续依赖证明明确使用紧矩形上的一致连续性，并给出
   \((b-a)\sup_x|f(x,t)-f(x,t_0)|\) 估计。
2. 一致极限交换证明先调用一致极限保持可积性，再以
   \((b-a)\lVert f_n-f\rVert_\infty\) 控制积分误差。
3. 移动尖峰在 \([0,2/n]\) 上连续，峰高为 \(n\)，面积恒为 1；对每个固定
   \(x\in[0,1]\) 逐点趋于零，因此交换两操作分别得到 1 与 0。
4. 范围止于正常积分；无界区间只作为第 44 章后续接口，不借用后续积分理论。

## 内容与出版审查

- 5 个单元均具有完整元数据、稳定锚点和统一九段结构。
- 共 46 道稳定锚点习题、56 个折叠答案；每单元至少 2 个完整例题和 2 个即时检验。
- README、课程地图、依赖图谱和导航统一到 194 个核心单元、344 学时、第 42 章。
- 第 43 章没有正文或导航。

## LaTeX 与机器门禁

- 行间公式分隔符数量成对，行间公式内没有嵌套行内分隔符。
- 所有行内美元分隔符逐行成对；清除了补丁传递中出现的终端控制字符。
- 严格建站成功，五个单元生成页均包含 `arithmatex` 渲染节点。
- 483 项 Python 测试通过。
- `python3.12 scripts/check_content.py`、`zensical build --strict`、
  `python3.12 scripts/check_site.py` 与 `git diff --check` 均通过。

机器门禁用于防止结构和公式标记回退，不替代上述数学推理审查。
